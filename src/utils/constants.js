// 项目常量配置

// 滚动配置
export const SCROLL_CONFIG = {
  SNAP_POINTS: [0, 40, 100],
  SNAP_THRESHOLD: 20,
  SCROLL_SPEED: 0.5,
  LOCK_DURATION: 1000,
};

// 动画配置
export const ANIMATION_CONFIG = {
  TRANSITION_DURATION: 300,
  EASE_FUNCTION: 'ease-in-out',
};

// 断点配置
export const BREAKPOINTS = {
  MOBILE: 768,
  TABLET: 1024,
  DESKTOP: 1200,
}; 